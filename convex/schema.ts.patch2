--- convex/schema.ts
+++ convex/schema.ts
@@ -58,5 +58,15 @@
     createdBy: v.string(),
     createdAt: v.number(),
   }).index("by_meeting", ["meetingId"]),
+
+  breakoutRooms: defineTable({
+    meetingId: v.string(),
+    rooms: v.array(v.object({
+      id: v.string(),
+      name: v.string(),
+      participants: v.array(v.string()) // User IDs / identities
+    })),
+    status: v.union(v.literal("active"), v.literal("closed")),
+  }).index("by_meeting", ["meetingId", "status"]),
 });
